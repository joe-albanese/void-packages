From: "Lukas F. Hartmann" <lukas@mntre.com>
Date: Wed, 30 Apr 2025 06:11:48 +0200
Subject: Linux 6.7

Forwarded: not-needed
---
 CORE/CLD_TXRX/TXRX/ol_txrx_internal.h              |   2 +-
 CORE/EPPING/src/epping_rx.c                        |  20 +++-
 CORE/EPPING/src/epping_txrx.c                      |   7 +-
 CORE/HDD/src/wlan_hdd_assoc.c                      |   4 +-
 CORE/HDD/src/wlan_hdd_cfg80211.c                   | 110 ++++++---------------
 CORE/HDD/src/wlan_hdd_hostapd.c                    |  15 +--
 CORE/HDD/src/wlan_hdd_main.c                       |  51 +++-------
 CORE/HDD/src/wlan_hdd_memdump.c                    |   2 +-
 CORE/HDD/src/wlan_hdd_softap_tx_rx.c               |   6 +-
 CORE/HDD/src/wlan_hdd_tx_rx.c                      |  19 +++-
 CORE/MAC/src/include/parserApi.h                   |   2 +-
 CORE/MAC/src/include/utilsApi.h                    |   2 +-
 CORE/SERVICES/BMI/bmi.c                            |  32 ------
 CORE/SERVICES/COMMON/osapi_linux.h                 |  17 ----
 CORE/SERVICES/HIF/PCIe/copy_engine.c               |  24 -----
 CORE/SERVICES/HIF/PCIe/hif_pci.c                   |  45 ---------
 CORE/SERVICES/HIF/PCIe/if_pci.c                    |  32 ------
 CORE/SERVICES/HIF/ath_procfs.c                     |   6 +-
 CORE/SERVICES/HIF/sdio/hif_sdio_recv.c             |   3 +-
 CORE/SERVICES/HIF/sdio/linux/native_sdio/src/hif.c |   4 +-
 CORE/SVC/src/logging/wlan_logging_sock_svc.c       |   2 +-
 CORE/SYS/legacy/src/system/inc/sysDebug.h          |   2 +-
 CORE/SYS/legacy/src/utils/inc/utilsParser.h        |   2 +-
 CORE/SYS/legacy/src/utils/src/dot11f.c             |   2 +-
 CORE/SYS/legacy/src/utils/src/logApi.c             |   2 +-
 CORE/UTILS/PKTLOG/linux_ac.c                       |  14 ++-
 CORE/VOSS/inc/vos_trace.h                          |   2 +-
 CORE/VOSS/src/vos_nvitem.c                         |   4 +-
 CORE/VOSS/src/vos_sched.c                          |   6 +-
 CORE/VOSS/src/vos_utils.c                          |   6 +-
 CORE/WDA/src/wlan_qct_wda_debug.h                  |   2 +-
 Kbuild                                             |   8 +-
 Makefile                                           |   4 +-
 33 files changed, 135 insertions(+), 324 deletions(-)

diff --git a/CORE/CLD_TXRX/TXRX/ol_txrx_internal.h b/CORE/CLD_TXRX/TXRX/ol_txrx_internal.h
index 73bb08e..ae8545b 100644
--- a/CORE/CLD_TXRX/TXRX/ol_txrx_internal.h
+++ b/CORE/CLD_TXRX/TXRX/ol_txrx_internal.h
@@ -106,7 +106,7 @@ extern unsigned g_txrx_print_level;
 
 #ifdef TXRX_PRINT_ENABLE
 
-#include <stdarg.h>       /* va_list */
+#include <linux/stdarg.h>       /* va_list */
 #include <adf_os_types.h> /* adf_os_vprint */
 
 /* Supress 4296 - expression is always true
diff --git a/CORE/EPPING/src/epping_rx.c b/CORE/EPPING/src/epping_rx.c
index 978ad46..6caf495 100644
--- a/CORE/EPPING/src/epping_rx.c
+++ b/CORE/EPPING/src/epping_rx.c
@@ -164,7 +164,10 @@ void epping_rx(void *ctx, HTC_PACKET *pPacket)
                    sizeof(unsigned int));
                epping_set_kperf_flag(pAdapter, eid, false);
                adf_net_buf_debug_release_skb(pktSkb);
-               netif_rx_ni(pktSkb);
+               //netif_rx_ni(pktSkb);
+               rcu_read_lock_bh();
+               netif_receive_skb(pktSkb);
+               rcu_read_unlock_bh();
                break;
             case 0: /* RXPERF hard code 0 in FW */
                adf_nbuf_free(pktSkb);
@@ -177,11 +180,17 @@ void epping_rx(void *ctx, HTC_PACKET *pPacket)
             case EPPING_CMD_CAPTURE_RECV_CNT:
                epping_set_kperf_flag(pAdapter, eid, false);
                adf_net_buf_debug_release_skb(pktSkb);
-               netif_rx_ni(pktSkb);
+               //netif_rx_ni(pktSkb);
+               rcu_read_lock_bh();
+               netif_receive_skb(pktSkb);
+               rcu_read_unlock_bh();
                break;
             default:
                adf_net_buf_debug_release_skb(pktSkb);
-               netif_rx_ni(pktSkb);
+               //netif_rx_ni(pktSkb);
+               rcu_read_lock_bh();
+               netif_receive_skb(pktSkb);
+               rcu_read_unlock_bh();
                pEpping_ctx->kperf_num_rx_recv[eid]++;
                if ((pAdapter->stats.rx_packets % EPPING_STATS_LOG_COUNT) == 0) {
                    EPPING_LOG(VOS_TRACE_LEVEL_FATAL, "%s: total_rx_pkts = %lu",
@@ -191,7 +200,10 @@ void epping_rx(void *ctx, HTC_PACKET *pPacket)
             }
          } else {
             adf_net_buf_debug_release_skb(pktSkb);
-            netif_rx_ni(pktSkb);
+            //netif_rx_ni(pktSkb);
+            rcu_read_lock_bh();
+            netif_receive_skb(pktSkb);
+            rcu_read_unlock_bh();
             if ((pAdapter->stats.rx_packets % EPPING_STATS_LOG_COUNT) == 0) {
                EPPING_LOG(VOS_TRACE_LEVEL_FATAL, "%s: total_rx_pkts = %lu",
                   __func__, pAdapter->stats.rx_packets);
diff --git a/CORE/EPPING/src/epping_txrx.c b/CORE/EPPING/src/epping_txrx.c
index 49e260a..a08f04a 100644
--- a/CORE/EPPING/src/epping_txrx.c
+++ b/CORE/EPPING/src/epping_txrx.c
@@ -252,7 +252,8 @@ static int epping_set_mac_address(struct net_device *dev, void *addr)
    struct sockaddr *psta_mac_addr = addr;
    vos_mem_copy(&pAdapter->macAddressCurrent,
                 psta_mac_addr->sa_data, ETH_ALEN);
-   vos_mem_copy(dev->dev_addr, psta_mac_addr->sa_data, ETH_ALEN);
+   //vos_mem_copy(dev->dev_addr, psta_mac_addr->sa_data, ETH_ALEN);
+   eth_hw_addr_set(dev, psta_mac_addr->sa_data);
    return 0;
 }
 
@@ -397,7 +398,9 @@ epping_adapter_t *epping_add_adapter(epping_context_t *pEpping_ctx,
    pAdapter->dev = dev;
    pAdapter->pEpping_ctx = pEpping_ctx;
    pAdapter->device_mode = device_mode; /* station, SAP, etc */
-   vos_mem_copy(dev->dev_addr, (void *)macAddr, sizeof(tSirMacAddr));
+   //vos_mem_copy(dev->dev_addr, (void *)macAddr, sizeof(tSirMacAddr));
+   eth_hw_addr_set(dev, (void *)macAddr);
+
    vos_mem_copy(pAdapter->macAddressCurrent.bytes,
                 macAddr, sizeof(tSirMacAddr));
    adf_os_spinlock_init(&pAdapter->data_lock);
diff --git a/CORE/HDD/src/wlan_hdd_assoc.c b/CORE/HDD/src/wlan_hdd_assoc.c
index c09f9b1..b1cbf95 100644
--- a/CORE/HDD/src/wlan_hdd_assoc.c
+++ b/CORE/HDD/src/wlan_hdd_assoc.c
@@ -2155,7 +2155,7 @@ static void hdd_send_roamed_ind(struct net_device *dev,
 {
 	struct cfg80211_roam_info info = {0};
 
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
 	info.links[0].bss = bss;
 #else
 	info.bss = bss;
@@ -6461,7 +6461,7 @@ static int __iw_get_ap_address(struct net_device *dev,
     }
     else
     {
-        memset(wrqu->ap_addr.sa_data,0,sizeof(wrqu->ap_addr.sa_data));
+        memset(wrqu->ap_addr.sa_data,0,sizeof(pHddStaCtx->conn_info.bssId));
     }
     EXIT();
     return 0;
diff --git a/CORE/HDD/src/wlan_hdd_cfg80211.c b/CORE/HDD/src/wlan_hdd_cfg80211.c
index 8687a6d..7ff80f4 100644
--- a/CORE/HDD/src/wlan_hdd_cfg80211.c
+++ b/CORE/HDD/src/wlan_hdd_cfg80211.c
@@ -1642,7 +1642,7 @@ static int __is_driver_dfs_capable(struct wiphy *wiphy,
 
 #if (LINUX_VERSION_CODE > KERNEL_VERSION(3,4,0)) || \
     defined (DFS_MASTER_OFFLOAD_IND_SUPPORT) || defined(WITH_BACKPORTS)
-    dfs_capability = !!(wiphy->flags & WIPHY_FLAG_DFS_OFFLOAD);
+    //dfs_capability = !!(wiphy->flags & WIPHY_FLAG_DFS_OFFLOAD);
 #endif
 
     temp_skbuff = cfg80211_vendor_cmd_alloc_reply_skb(wiphy, sizeof(u32) +
@@ -16786,7 +16786,7 @@ int wlan_hdd_cfg80211_init(struct device *dev,
 #if (LINUX_VERSION_CODE > KERNEL_VERSION(3,4,0)) || \
     defined (DFS_MASTER_OFFLOAD_IND_SUPPORT) || defined(WITH_BACKPORTS)
     if (pCfg->enableDFSMasterCap) {
-        wiphy->flags |= WIPHY_FLAG_DFS_OFFLOAD;
+      //wiphy->flags |= WIPHY_FLAG_DFS_OFFLOAD;
     }
 #endif
 
@@ -19543,7 +19543,7 @@ static int wlan_hdd_cfg80211_del_beacon(struct wiphy *wiphy,
  */
 static int wlan_hdd_cfg80211_stop_ap(struct wiphy *wiphy,
 					struct net_device *dev
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
 					, unsigned int link_id
 #endif
 					)
@@ -19949,10 +19949,11 @@ static void wlan_hdd_cfg80211_stop_p2p_dev(struct wiphy *wiphy,
 
 static int __wlan_hdd_cfg80211_change_beacon(struct wiphy *wiphy,
                                            struct net_device *dev,
-                                           struct cfg80211_beacon_data *params)
+                                           struct cfg80211_ap_update *params)
 {
     hdd_adapter_t *pAdapter =  WLAN_HDD_GET_PRIV_PTR(dev);
     hdd_context_t *pHddCtx;
+    struct cfg80211_beacon_data *bcon = &params->beacon;
     beacon_data_t *old,*new;
     int status;
 
@@ -19998,7 +19999,7 @@ static int __wlan_hdd_cfg80211_change_beacon(struct wiphy *wiphy,
         return -EINVAL;
     }
 
-    status = wlan_hdd_cfg80211_alloc_new_beacon(pAdapter, &new, params, 0);
+    status = wlan_hdd_cfg80211_alloc_new_beacon(pAdapter, &new, bcon, 0);
 
     if (status != VOS_STATUS_SUCCESS) {
         hddLog(VOS_TRACE_LEVEL_FATAL, FL("new beacon alloc failed"));
@@ -20006,7 +20007,7 @@ static int __wlan_hdd_cfg80211_change_beacon(struct wiphy *wiphy,
     }
 
     pAdapter->sessionCtx.ap.beacon = new;
-    status = wlan_hdd_cfg80211_start_bss(pAdapter, params, NULL, 0, 0);
+    status = wlan_hdd_cfg80211_start_bss(pAdapter, bcon, NULL, 0, 0);
 
     EXIT();
     return status;
@@ -20021,7 +20022,7 @@ static int __wlan_hdd_cfg80211_change_beacon(struct wiphy *wiphy,
  */
 static int wlan_hdd_cfg80211_change_beacon(struct wiphy *wiphy,
 					struct net_device *dev,
-					struct cfg80211_beacon_data *params)
+					struct cfg80211_ap_update *params)
 {
 	int ret;
 
@@ -20302,30 +20303,20 @@ static int __wlan_hdd_cfg80211_change_iface(struct wiphy *wiphy,
                 }
             }
 
-            if ((WLAN_HDD_SOFTAP == pAdapter->device_mode) &&
-                (pConfig->apRandomBssidEnabled)) {
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 17, 0))
-                u8 addr[MAX_ADDR_LEN];
-                get_random_bytes(&addr[3], 3);
-                addr[0] = 0x02;
-                addr[1] = 0x1A;
-                addr[2] = 0x11;
-                addr[3] |= 0xF0;
-                dev_addr_mod(ndev, 0, addr, 6);
-#else
+            /*if ((WLAN_HDD_SOFTAP == pAdapter->device_mode) &&
+              (pConfig->apRandomBssidEnabled)) {*/
                 /* To meet Android requirements create a randomized
                    MAC address of the form 02:1A:11:Fx:xx:xx */
-                get_random_bytes(&ndev->dev_addr[3], 3);
+            /*    get_random_bytes(&ndev->dev_addr[3], 3);
                 ndev->dev_addr[0] = 0x02;
                 ndev->dev_addr[1] = 0x1A;
                 ndev->dev_addr[2] = 0x11;
                 ndev->dev_addr[3] |= 0xF0;
-#endif
                 memcpy(pAdapter->macAddressCurrent.bytes, ndev->dev_addr,
                        VOS_MAC_ADDR_SIZE);
                 pr_info("wlan: Generated HotSpot BSSID "MAC_ADDRESS_STR"\n",
                         MAC_ADDR_ARRAY(ndev->dev_addr));
-            }
+            }*/
 
             hdd_set_ap_ops(pAdapter->dev);
 
@@ -20934,24 +20925,12 @@ static int __wlan_hdd_change_station(struct wiphy *wiphy,
                 vos_mem_copy(StaParams.extn_capability, params->ext_capab,
                              params->ext_capab_len);
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
-            if (NULL != params->link_sta_params.ht_capa) {
-#else
-            if (NULL != params->ht_capa) {
-#endif
-                StaParams.htcap_present = 1;
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
-                vos_mem_copy(&StaParams.HTCap, params->link_sta_params.ht_capa, sizeof(tSirHTCap));
-#else
-                vos_mem_copy(&StaParams.HTCap, params->ht_capa, sizeof(tSirHTCap));
-#endif
-            }
+            //if (NULL != params->ht_capa) {
+            //    StaParams.htcap_present = 1;
+            //    vos_mem_copy(&StaParams.HTCap, params->ht_capa, sizeof(tSirHTCap));
+            //}
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
-            StaParams.supported_rates_len = params->link_sta_params.supported_rates_len;
-#else
-            StaParams.supported_rates_len = params->supported_rates_len;
-#endif
+            //StaParams.supported_rates_len = params->supported_rates_len;
 
             /*
              * Note : The Maximum sizeof supported_rates sent by the Supplicant
@@ -20962,37 +20941,24 @@ static int __wlan_hdd_change_station(struct wiphy *wiphy,
 
             /* To avoid Data Corruption, set to max length
                to SIR_MAC_MAX_SUPP_RATES */
-            if (StaParams.supported_rates_len > SIR_MAC_MAX_SUPP_RATES)
+            /*if (StaParams.supported_rates_len > SIR_MAC_MAX_SUPP_RATES)
                 StaParams.supported_rates_len = SIR_MAC_MAX_SUPP_RATES;
 
             if (0 != StaParams.supported_rates_len) {
                 int i = 0;
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
-                vos_mem_copy(StaParams.supported_rates, params->link_sta_params.supported_rates,
-                             StaParams.supported_rates_len);
-#else
                 vos_mem_copy(StaParams.supported_rates, params->supported_rates,
                              StaParams.supported_rates_len);
-#endif
                 VOS_TRACE( VOS_MODULE_ID_HDD, VOS_TRACE_LEVEL_INFO,
                            "Supported Rates with Length %d", StaParams.supported_rates_len);
                 for (i=0; i < StaParams.supported_rates_len; i++)
                     VOS_TRACE( VOS_MODULE_ID_HDD, VOS_TRACE_LEVEL_INFO,
                                "[%d]: %0x", i, StaParams.supported_rates[i]);
-            }
+                               }*/
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
-            if (NULL != params->link_sta_params.vht_capa) {
-#else
-            if (NULL != params->vht_capa) {
-#endif
+            /*if (NULL != params->vht_capa) {
                 StaParams.vhtcap_present = 1;
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
-                vos_mem_copy(&StaParams.VHTCap, params->link_sta_params.vht_capa, sizeof(tSirVHTCap));
-#else
                 vos_mem_copy(&StaParams.VHTCap, params->vht_capa, sizeof(tSirVHTCap));
-#endif
-            }
+                }*/
 
             if (0 != params->ext_capab_len ) {
                 /*Define A Macro : TODO Sunil*/
@@ -21005,14 +20971,10 @@ static int __wlan_hdd_change_station(struct wiphy *wiphy,
                 }
             }
 
-            if (pHddCtx->cfg_ini->fEnableTDLSWmmMode &&
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
-                (params->link_sta_params.ht_capa || params->link_sta_params.vht_capa ||
-#else
-                (params->ht_capa || params->vht_capa ||
-#endif
-                (params->sta_flags_set & BIT(NL80211_STA_FLAG_WME))))
-                is_qos_wmm_sta = true;
+            //if (pHddCtx->cfg_ini->fEnableTDLSWmmMode &&
+            //    (params->ht_capa || params->vht_capa ||
+            //    (params->sta_flags_set & BIT(NL80211_STA_FLAG_WME))))
+            //    is_qos_wmm_sta = true;
 
             hddLog(VOS_TRACE_LEVEL_INFO,
                    FL("%s: TDLS Peer is QOS capable is_qos_wmm_sta= %d HTcapPresent = %d"),
@@ -21435,7 +21397,7 @@ static int __wlan_hdd_cfg80211_add_key( struct wiphy *wiphy,
 
 static int wlan_hdd_cfg80211_add_key( struct wiphy *wiphy,
                                       struct net_device *ndev,
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
                                       int link_id,
 #endif
                                       u8 key_index, bool pairwise,
@@ -21532,7 +21494,7 @@ static int __wlan_hdd_cfg80211_get_key(
 static int wlan_hdd_cfg80211_get_key(
                         struct wiphy *wiphy,
                         struct net_device *ndev,
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
                         int link_id,
 #endif
                         u8 key_index, bool pairwise,
@@ -21679,7 +21641,7 @@ static int __wlan_hdd_cfg80211_del_key(struct wiphy *wiphy,
  */
 static int wlan_hdd_cfg80211_del_key(struct wiphy *wiphy,
 					struct net_device *dev,
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
 					int link_id,
 #endif
 					u8 key_index,
@@ -21817,7 +21779,7 @@ static int __wlan_hdd_cfg80211_set_default_key( struct wiphy *wiphy,
 
 static int wlan_hdd_cfg80211_set_default_key( struct wiphy *wiphy,
                                               struct net_device *ndev,
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
                                               int link_id,
 #endif
                                               u8 key_index,
@@ -25306,15 +25268,7 @@ static int __wlan_hdd_cfg80211_connect( struct wiphy *wiphy,
                             req->bssid, req->ssid,
                             req->ssid_len);
                 if (bss) {
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 18, 0))
-                    struct cfg80211_assoc_failure data;
-                    data.ap_mld_addr = NULL;
-                    data.bss[0] = bss;
-                    data.timeout = true;
-                    cfg80211_assoc_failure(ndev, &data);
-#else
-                    cfg80211_assoc_timeout(ndev, bss);
-#endif
+                  //cfg80211_assoc_timeout(ndev, bss);
                     return -ETIMEDOUT;
                 }
             }
@@ -28084,7 +28038,7 @@ static int __wlan_hdd_set_default_mgmt_key(struct wiphy *wiphy,
  */
 static int wlan_hdd_set_default_mgmt_key(struct wiphy *wiphy,
 					   struct net_device *netdev,
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
 					   int link_id,
 #endif
 					   u8 key_index)
@@ -32019,7 +31973,7 @@ __wlan_hdd_cfg80211_set_ap_channel_width(struct wiphy *wiphy,
 static int
 wlan_hdd_cfg80211_set_ap_channel_width(struct wiphy *wiphy,
 				       struct net_device *dev,
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
 				       unsigned int link_id,
 #endif
 				       struct cfg80211_chan_def *chandef)
diff --git a/CORE/HDD/src/wlan_hdd_hostapd.c b/CORE/HDD/src/wlan_hdd_hostapd.c
index 75b8b73..d0f8071 100644
--- a/CORE/HDD/src/wlan_hdd_hostapd.c
+++ b/CORE/HDD/src/wlan_hdd_hostapd.c
@@ -1068,7 +1068,9 @@ static int __hdd_hostapd_set_mac_address(struct net_device *dev, void *addr)
    if (0 != ret)
        return ret;
 
-   memcpy(dev->dev_addr, psta_mac_addr->sa_data, ETH_ALEN);
+   //memcpy(dev->dev_addr, psta_mac_addr->sa_data, ETH_ALEN);
+   eth_hw_addr_set(dev, psta_mac_addr->sa_data);
+
    EXIT();
    return 0;
 }
@@ -1569,7 +1571,11 @@ VOS_STATUS hdd_chan_change_notify(hdd_adapter_t *hostapd_adapter,
 		hdd_update_chandef(hostapd_adapter, &chandef, cb_mode);
 
 #if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#   if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0) && LINUX_VERSION_CODE < KERNEL_VERSION(6, 9, 0)
+	cfg80211_ch_switch_notify(dev, &chandef, 0, 0);
+#   else
 	cfg80211_ch_switch_notify(dev, &chandef, 0);
+#   endif
 #else
 	cfg80211_ch_switch_notify(dev, &chandef);
 #endif
@@ -9003,7 +9009,8 @@ hdd_adapter_t* hdd_wlan_create_ap_dev(hdd_context_t *pHddCtx,
         pWlanHostapdDev->mtu = HDD_DEFAULT_MTU;
         pWlanHostapdDev->tx_queue_len = HDD_NETDEV_TX_QUEUE_LEN;
 
-        vos_mem_copy(pWlanHostapdDev->dev_addr, (void *)macAddr,sizeof(tSirMacAddr));
+        //vos_mem_copy(pWlanHostapdDev->dev_addr, (void *)macAddr,sizeof(tSirMacAddr));
+        eth_hw_addr_set(pWlanHostapdDev, (void *)macAddr);
         vos_mem_copy(pHostapdAdapter->macAddressCurrent.bytes, (void *)macAddr, sizeof(tSirMacAddr));
 
         pHostapdAdapter->offloads_configured = FALSE;
@@ -9052,11 +9059,7 @@ VOS_STATUS hdd_register_hostapd( hdd_adapter_t *pAdapter, tANI_U8 rtnl_lock_held
             return VOS_STATUS_E_FAILURE;
          }
       }
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 12, 0))
-      if (cfg80211_register_netdevice(dev))
-#else
       if (register_netdevice(dev))
-#endif
       {
          hddLog(VOS_TRACE_LEVEL_FATAL,
                 "%s:Failed:register_netdevice", __func__);
diff --git a/CORE/HDD/src/wlan_hdd_main.c b/CORE/HDD/src/wlan_hdd_main.c
index c1fa7f6..6f3b20c 100644
--- a/CORE/HDD/src/wlan_hdd_main.c
+++ b/CORE/HDD/src/wlan_hdd_main.c
@@ -8703,11 +8703,7 @@ static int __hdd_ioctl(struct net_device *dev, struct ifreq *ifr, int cmd)
  *
  * Return: 0 for success and error number for failure.
  */
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 15, 0)
-static int hdd_siocdevprivate(struct net_device *dev, struct ifreq *ifr, void __user *data, int cmd)
-#else
 static int hdd_ioctl(struct net_device *dev, struct ifreq *ifr, int cmd)
-#endif
 {
 	int ret;
 
@@ -10769,11 +10765,8 @@ static int __hdd_set_mac_address(struct net_device *dev, void *addr)
 		return ret;
 
 	memcpy(&pAdapter->macAddressCurrent, psta_mac_addr->sa_data, ETH_ALEN);
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 17, 0))
-	dev_addr_mod(dev, 0, psta_mac_addr->sa_data, ETH_ALEN);
-#else
-	memcpy(dev->dev_addr, psta_mac_addr->sa_data, ETH_ALEN);
-#endif
+	//memcpy(dev->dev_addr, psta_mac_addr->sa_data, ETH_ALEN);
+  eth_hw_addr_set(dev, psta_mac_addr->sa_data);
 
 	EXIT();
 	return 0;
@@ -10992,11 +10985,7 @@ static struct net_device_ops wlan_drv_ops = {
       .ndo_start_xmit = hdd_hard_start_xmit,
       .ndo_tx_timeout = hdd_tx_timeout,
       .ndo_get_stats = hdd_stats,
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 15, 0)
-      .ndo_siocdevprivate = hdd_siocdevprivate,
-#else
       .ndo_do_ioctl = hdd_ioctl,
-#endif
       .ndo_set_mac_address = hdd_set_mac_address,
       .ndo_select_queue    = hdd_select_queue,
 #ifdef WLAN_FEATURE_PACKET_FILTERING
@@ -11244,11 +11233,9 @@ static hdd_adapter_t* hdd_alloc_station_adapter(hdd_context_t *pHddCtx,
       strlcpy(pAdapter->ifname, name, IFNAMSIZ);
 #endif
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 17, 0))
-      dev_addr_mod(pWlanDev, 0, (void *)macAddr, sizeof(tSirMacAddr));
-#else
-      vos_mem_copy(pWlanDev->dev_addr, (void *)macAddr, sizeof(tSirMacAddr));
-#endif
+      //vos_mem_copy(pWlanDev->dev_addr, (void *)macAddr, sizeof(tSirMacAddr));
+      eth_hw_addr_set(pWlanDev, (void *)macAddr);
+
       vos_mem_copy( pAdapter->macAddressCurrent.bytes, macAddr, sizeof(tSirMacAddr));
       pWlanDev->watchdog_timeo = HDD_TX_TIMEOUT;
       /*
@@ -11319,12 +11306,10 @@ static hdd_adapter_t *hdd_alloc_monitor_adapter(hdd_context_t *pHddCtx,
 	   /* Init the net_device structure */
 	   strlcpy(pwlan_dev->name, name, IFNAMSIZ);
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 17, 0))
-	   dev_addr_mod(pwlan_dev, 0, (void *)macAddr, sizeof(tSirMacAddr));
-#else
-	   vos_mem_copy(pwlan_dev->dev_addr,
-			(void *)macAddr, sizeof(tSirMacAddr));
-#endif
+	   //vos_mem_copy(pwlan_dev->dev_addr,
+     //	(void *)macAddr, sizeof(tSirMacAddr));
+     eth_hw_addr_set(pwlan_dev, (void *)macAddr);
+
 	   vos_mem_copy(pAdapter->macAddressCurrent.bytes,
 			macAddr, sizeof(tSirMacAddr));
 	   pwlan_dev->watchdog_timeo = HDD_TX_TIMEOUT;
@@ -11372,11 +11357,7 @@ VOS_STATUS hdd_register_interface( hdd_adapter_t *pAdapter, tANI_U8 rtnl_lock_he
             return VOS_STATUS_E_FAILURE;
          }
       }
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 12, 0))
-      if (cfg80211_register_netdevice(pWlanDev))
-#else
       if (register_netdevice(pWlanDev))
-#endif
       {
          hddLog(VOS_TRACE_LEVEL_ERROR,"%s:Failed:register_netdev",__func__);
          return VOS_STATUS_E_FAILURE;
@@ -11758,11 +11739,7 @@ void hdd_cleanup_adapter(hdd_context_t *pHddCtx, hdd_adapter_t *pAdapter,
 
    if (test_bit(NET_DEVICE_REGISTERED, &pAdapter->event_flags)) {
       if (rtnl_held) {
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 12, 0))
-         cfg80211_unregister_netdevice(pWlanDev);
-#else
          unregister_netdevice(pWlanDev);
-#endif
       } else {
          unregister_netdev(pWlanDev);
       }
@@ -12627,11 +12604,7 @@ err_add_adapter_back:
 
 err_malloc_adapter_node:
 	if (rtnl_held)
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 12, 0))
-		cfg80211_unregister_netdevice(adapter->dev);
-#else
 		unregister_netdevice(adapter->dev);
-#endif
 	else
 		unregister_netdev(adapter->dev);
 
@@ -13480,7 +13453,7 @@ static void hdd_connect_done(struct net_device *dev, const u8 *bssid,
     struct cfg80211_connect_resp_params fils_params;
     vos_mem_zero(&fils_params, sizeof(fils_params));
 
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
     fils_params.links[0].bssid = bssid;
 #else
     fils_params.bssid = bssid;
@@ -13496,7 +13469,7 @@ static void hdd_connect_done(struct net_device *dev, const u8 *bssid,
         fils_params.req_ie_len = req_ie_len;
         fils_params.resp_ie = resp_ie;
         fils_params.resp_ie_len = resp_ie_len;
-#if defined(FORCE_MLO_SUPPORT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0))
+#if defined(FORCE_MLO_SUPPORT)
         fils_params.links[0].bss = bss;
 #else
         fils_params.bss = bss;
@@ -18382,9 +18355,7 @@ success:
 
 /* accommodate the request firmware bin time out 2 min */
 #define REQUEST_FWR_TIMEOUT 120000
-#ifndef HDD_WLAN_START_WAIT_TIME
 #define HDD_WLAN_START_WAIT_TIME (VOS_WDA_TIMEOUT + 5000 + REQUEST_FWR_TIMEOUT)
-#endif
 /**
  * hdd_hif_register_driver() - API for HDD to register with HIF
  *
diff --git a/CORE/HDD/src/wlan_hdd_memdump.c b/CORE/HDD/src/wlan_hdd_memdump.c
index e6a6478..6f4ea14 100644
--- a/CORE/HDD/src/wlan_hdd_memdump.c
+++ b/CORE/HDD/src/wlan_hdd_memdump.c
@@ -68,7 +68,7 @@ static void *memdump_get_file_data(struct file *file)
 {
 	void *hdd_ctx;
 
-	hdd_ctx = PDE_DATA(file_inode(file));
+	hdd_ctx = pde_data(file_inode(file));
 	return hdd_ctx;
 }
 #else
diff --git a/CORE/HDD/src/wlan_hdd_softap_tx_rx.c b/CORE/HDD/src/wlan_hdd_softap_tx_rx.c
index 3b34606..eb6eccb 100644
--- a/CORE/HDD/src/wlan_hdd_softap_tx_rx.c
+++ b/CORE/HDD/src/wlan_hdd_softap_tx_rx.c
@@ -1000,7 +1000,10 @@ VOS_STATUS hdd_softap_rx_packet_cbk(v_VOID_t *vosContext,
           * This is the last packet on the chain
           * Scheduling rx sirq
           */
-         rxstat = netif_rx_ni(skb);
+         //rxstat = netif_rx_ni(skb);
+         rcu_read_lock_bh();
+         netif_receive_skb(skb);
+         rcu_read_unlock_bh();
       }
 
       if (NET_RX_SUCCESS == rxstat)
@@ -1329,4 +1332,3 @@ VOS_STATUS hdd_softap_GetStaId(hdd_adapter_t *pAdapter, v_MACADDR_t *pMacAddress
 
     return VOS_STATUS_E_FAILURE;
 }
-
diff --git a/CORE/HDD/src/wlan_hdd_tx_rx.c b/CORE/HDD/src/wlan_hdd_tx_rx.c
index 7046632..b3ea6e2 100644
--- a/CORE/HDD/src/wlan_hdd_tx_rx.c
+++ b/CORE/HDD/src/wlan_hdd_tx_rx.c
@@ -1279,7 +1279,10 @@ VOS_STATUS hdd_mon_rx_packet_cbk(v_VOID_t *vos_ctx, adf_nbuf_t rx_buf,
 			 * This is the last packet on the chain
 			 * Scheduling rx sirq
 			 */
-			rxstat = netif_rx_ni(skb);
+			//rxstat = netif_rx_ni(skb);
+      rcu_read_lock_bh();
+      netif_receive_skb(skb);
+      rcu_read_unlock_bh();
 		}
 
 		if (NET_RX_SUCCESS == rxstat)
@@ -1344,7 +1347,10 @@ VOS_STATUS hdd_vir_mon_rx_cbk(v_VOID_t *vos_ctx, adf_nbuf_t rx_buf,
 			 * This is the last packet on the chain
 			 * Scheduling rx sirq
 			 */
-			rxstat = netif_rx_ni(skb);
+			//rxstat = netif_rx_ni(skb);
+      rcu_read_lock_bh();
+      netif_receive_skb(skb);
+      rcu_read_unlock_bh();
 		}
 
 		skb = skb_next;
@@ -1476,8 +1482,10 @@ VOS_STATUS hdd_rx_packet_cbk(v_VOID_t *vosContext,
    while (NULL != skb) {
       skb_next = skb->next;
 
+      // FIXME HACK
       if (((pHddStaCtx->conn_info.proxyARPService) &&
-         cfg80211_is_gratuitous_arp_unsolicited_na(skb)) ||
+           false
+           /*cfg80211_is_gratuitous_arp_unsolicited_na(skb)*/) ||
          vos_is_load_unload_in_progress(VOS_MODULE_ID_VOSS, NULL)) {
             ++pAdapter->hdd_stats.hddTxRxStats.rxDropped[cpu_index];
             VOS_TRACE(VOS_MODULE_ID_HDD_DATA, VOS_TRACE_LEVEL_INFO,
@@ -1588,7 +1596,10 @@ VOS_STATUS hdd_rx_packet_cbk(v_VOID_t *vosContext,
            * This is the last packet on the chain
            * Scheduling rx sirq
            */
-          rxstat = netif_rx_ni(skb);
+          //rxstat = netif_rx_ni(skb);
+          rcu_read_lock_bh();
+          netif_receive_skb(skb);
+          rcu_read_unlock_bh();
       }
 
       if (NET_RX_SUCCESS == rxstat)
diff --git a/CORE/MAC/src/include/parserApi.h b/CORE/MAC/src/include/parserApi.h
index 158ed57..1f27a8c 100644
--- a/CORE/MAC/src/include/parserApi.h
+++ b/CORE/MAC/src/include/parserApi.h
@@ -38,7 +38,7 @@
 #ifndef __PARSE_H__
 #define __PARSE_H__
 
-#include <stdarg.h>
+#include <linux/stdarg.h>
 #include "sirMacPropExts.h"
 #include "dot11f.h"
 #ifdef WLAN_FEATURE_VOWIFI_11R
diff --git a/CORE/MAC/src/include/utilsApi.h b/CORE/MAC/src/include/utilsApi.h
index d81ee00..ae1d5d8 100644
--- a/CORE/MAC/src/include/utilsApi.h
+++ b/CORE/MAC/src/include/utilsApi.h
@@ -39,7 +39,7 @@
 #ifndef __UTILSAPI_H
 #define __UTILSAPI_H
 
-#include <stdarg.h>
+#include <linux/stdarg.h>
 #include <sirCommon.h>
 #include "aniGlobal.h"
 #include "utilsGlobal.h"
diff --git a/CORE/SERVICES/BMI/bmi.c b/CORE/SERVICES/BMI/bmi.c
index e384604..b0d9013 100644
--- a/CORE/SERVICES/BMI/bmi.c
+++ b/CORE/SERVICES/BMI/bmi.c
@@ -102,15 +102,9 @@ BMIInit(struct ol_softc *scn)
                 (A_UCHAR *)A_MALLOC(MAX_BMI_CMDBUF_SZ);
 #else
         scn->pBMICmdBuf =
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
                 (A_UCHAR *)pci_alloc_consistent(scn->sc_osdev->bdev,
                                     MAX_BMI_CMDBUF_SZ,
                                     &scn->BMICmd_pa);
-#else
-            (A_UCHAR *)dma_alloc_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                          MAX_BMI_CMDBUF_SZ,
-                                          &scn->BMICmd_pa, GFP_ATOMIC);
-#endif
 #endif
         ASSERT(scn->pBMICmdBuf);
     }
@@ -121,15 +115,9 @@ BMIInit(struct ol_softc *scn)
                 (A_UCHAR *)A_MALLOC(MAX_BMI_CMDBUF_SZ);
 #else
         scn->pBMIRspBuf =
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
                 (A_UCHAR *)pci_alloc_consistent(scn->sc_osdev->bdev,
                                 MAX_BMI_CMDBUF_SZ,
                                 &scn->BMIRsp_pa);
-#else
-            (A_UCHAR *)dma_alloc_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                          MAX_BMI_CMDBUF_SZ,
-                                          &scn->BMIRsp_pa, GFP_ATOMIC);
-#endif
 #endif
         ASSERT(scn->pBMIRspBuf);
     }
@@ -144,13 +132,8 @@ BMICleanup(struct ol_softc *scn)
 #ifndef HIF_PCI
         A_FREE(scn->pBMICmdBuf );
 #else
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
         pci_free_consistent(scn->sc_osdev->bdev, MAX_BMI_CMDBUF_SZ,
                         scn->pBMICmdBuf, scn->BMICmd_pa);
-#else
-        dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                          MAX_BMI_CMDBUF_SZ, scn->pBMICmdBuf, scn->BMICmd_pa);
-#endif
 #endif
         scn->pBMICmdBuf = NULL;
         scn->BMICmd_pa = 0;
@@ -160,13 +143,8 @@ BMICleanup(struct ol_softc *scn)
 #ifndef HIF_PCI
         A_FREE(scn->pBMIRspBuf);
 #else
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
         pci_free_consistent(scn->sc_osdev->bdev, MAX_BMI_CMDBUF_SZ,
                         scn->pBMIRspBuf, scn->BMIRsp_pa);
-#else
-        dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                          MAX_BMI_CMDBUF_SZ, scn->pBMIRspBuf, scn->BMIRsp_pa);
-#endif
 #endif
         scn->pBMIRspBuf = NULL;
         scn->BMIRsp_pa = 0;
@@ -222,13 +200,8 @@ BMIDone(HIF_DEVICE *device, struct ol_softc *scn)
 #ifndef HIF_PCI
         A_FREE(scn->pBMICmdBuf);
 #else
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
         pci_free_consistent(scn->sc_osdev->bdev, MAX_BMI_CMDBUF_SZ,
                         scn->pBMICmdBuf, scn->BMICmd_pa);
-#else
-        dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                          MAX_BMI_CMDBUF_SZ, scn->pBMICmdBuf, scn->BMICmd_pa);
-#endif
 #endif
         scn->pBMICmdBuf = NULL;
         scn->BMICmd_pa = 0;
@@ -238,13 +211,8 @@ BMIDone(HIF_DEVICE *device, struct ol_softc *scn)
 #ifndef HIF_PCI
         A_FREE(scn->pBMIRspBuf);
 #else
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
         pci_free_consistent(scn->sc_osdev->bdev, MAX_BMI_CMDBUF_SZ,
                         scn->pBMIRspBuf, scn->BMIRsp_pa);
-#else
-        dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                          MAX_BMI_CMDBUF_SZ, scn->pBMIRspBuf, scn->BMIRsp_pa);
-#endif
 #endif
         scn->pBMIRspBuf = NULL;
         scn->BMIRsp_pa = 0;
diff --git a/CORE/SERVICES/COMMON/osapi_linux.h b/CORE/SERVICES/COMMON/osapi_linux.h
index 1c58d3b..cb7b3ac 100644
--- a/CORE/SERVICES/COMMON/osapi_linux.h
+++ b/CORE/SERVICES/COMMON/osapi_linux.h
@@ -69,23 +69,6 @@
 #define PREPACK
 #define POSTPACK                __ATTRIB_PACK
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 17, 0))
-#define complete_and_exit kthread_complete_and_exit
-#define PDE_DATA pde_data
-#endif
-
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 18, 0))
-static inline int netif_rx_ni(struct sk_buff *skb)
-{
-	return netif_rx(skb);
-}
-
-static inline int netif_rx_any_context(struct sk_buff *skb)
-{
-	return netif_rx(skb);
-}
-#endif
-
 /*
  * Endianes macros
  */
diff --git a/CORE/SERVICES/HIF/PCIe/copy_engine.c b/CORE/SERVICES/HIF/PCIe/copy_engine.c
index 375ffb7..29ff9e3 100644
--- a/CORE/SERVICES/HIF/PCIe/copy_engine.c
+++ b/CORE/SERVICES/HIF/PCIe/copy_engine.c
@@ -1673,15 +1673,9 @@ CE_init(struct hif_pci_softc *sc,
 
             /* Legacy platforms that do not support cache coherent DMA are unsupported */
             src_ring->base_addr_owner_space_unaligned =
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
                 pci_alloc_consistent(scn->sc_osdev->bdev,
                                     (nentries * sizeof(struct CE_src_desc) + CE_DESC_RING_ALIGN),
                                     &base_addr);
-#else
-                dma_alloc_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                   (nentries * sizeof(struct CE_src_desc) + CE_DESC_RING_ALIGN),
-                                   &base_addr, GFP_ATOMIC);
-#endif
             if (src_ring->base_addr_owner_space_unaligned == NULL) {
                 dev_err(&sc->pdev->dev, "ath ERROR: src ring has no DMA mem\n");
                 goto error_no_dma_mem;
@@ -1777,15 +1771,9 @@ CE_init(struct hif_pci_softc *sc,
 
             /* Legacy platforms that do not support cache coherent DMA are unsupported */
             dest_ring->base_addr_owner_space_unaligned =
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
                 pci_alloc_consistent(scn->sc_osdev->bdev,
                                     (nentries * sizeof(struct CE_dest_desc) + CE_DESC_RING_ALIGN),
                                     &base_addr);
-#else
-                dma_alloc_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                   (nentries * sizeof(struct CE_dest_desc) + CE_DESC_RING_ALIGN),
-                                   &base_addr, GFP_ATOMIC);
-#endif
             if (dest_ring->base_addr_owner_space_unaligned == NULL) {
                 dev_err(&sc->pdev->dev, "ath ERROR: dest ring has no DMA mem\n");
                 goto error_no_dma_mem;
@@ -1857,28 +1845,16 @@ CE_fini(struct CE_handle *copyeng)
         if (CE_state->src_ring->shadow_base_unaligned)
             A_FREE(CE_state->src_ring->shadow_base_unaligned);
         if (CE_state->src_ring->base_addr_owner_space_unaligned)
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
             pci_free_consistent(scn->sc_osdev->bdev,
                    (CE_state->src_ring->nentries * sizeof(struct CE_src_desc) + CE_DESC_RING_ALIGN),
                    CE_state->src_ring->base_addr_owner_space_unaligned, CE_state->src_ring->base_addr_CE_space);
-#else
-            dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                   (CE_state->src_ring->nentries * sizeof(struct CE_src_desc) + CE_DESC_RING_ALIGN),
-                   CE_state->src_ring->base_addr_owner_space_unaligned, CE_state->src_ring->base_addr_CE_space);
-#endif
         A_FREE(CE_state->src_ring);
     }
     if (CE_state->dest_ring) {
         if (CE_state->dest_ring->base_addr_owner_space_unaligned)
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
             pci_free_consistent(scn->sc_osdev->bdev,
                    (CE_state->dest_ring->nentries * sizeof(struct CE_dest_desc) + CE_DESC_RING_ALIGN),
                    CE_state->dest_ring->base_addr_owner_space_unaligned, CE_state->dest_ring->base_addr_CE_space);
-#else
-            dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                   (CE_state->dest_ring->nentries * sizeof(struct CE_dest_desc) + CE_DESC_RING_ALIGN),
-                   CE_state->dest_ring->base_addr_owner_space_unaligned, CE_state->dest_ring->base_addr_CE_space);
-#endif
         A_FREE(CE_state->dest_ring);
 
         /* epping */
diff --git a/CORE/SERVICES/HIF/PCIe/hif_pci.c b/CORE/SERVICES/HIF/PCIe/hif_pci.c
index 2f321b0..4aed580 100644
--- a/CORE/SERVICES/HIF/PCIe/hif_pci.c
+++ b/CORE/SERVICES/HIF/PCIe/hif_pci.c
@@ -1166,25 +1166,15 @@ HIFDiagReadMem(HIF_DEVICE *hif_device, A_UINT32 address, A_UINT8 *data, int nbyt
      *   2) Buffer in DMA-able space
      */
     orig_nbytes = nbytes;
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     data_buf = (A_UCHAR *)pci_alloc_consistent(scn->sc_osdev->bdev,
                                              orig_nbytes,
                                              &CE_data_base);
-#else
-    data_buf = (A_UCHAR *)dma_alloc_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                             orig_nbytes, &CE_data_base, GFP_ATOMIC);
-#endif
     if (!data_buf) {
         status = A_NO_MEMORY;
         goto done;
     }
     adf_os_mem_set(data_buf, 0, orig_nbytes);
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     pci_dma_sync_single_for_device(scn->sc_osdev->bdev, CE_data_base, orig_nbytes, PCI_DMA_FROMDEVICE);
-#else
-    dma_sync_single_for_device(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                               CE_data_base, orig_nbytes, DMA_FROM_DEVICE);
-#endif
 
     remaining_bytes = orig_nbytes;
     CE_data = CE_data_base;
@@ -1270,13 +1260,8 @@ done:
     }
 
     if (data_buf) {
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
        pci_free_consistent(scn->sc_osdev->bdev, orig_nbytes,
                         data_buf, CE_data_base);
-#else
-       dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                         orig_nbytes, data_buf, CE_data_base);
-#endif
     }
 
     AR_DEBUG_PRINTF(ATH_DEBUG_TRC, ("-%s\n",__FUNCTION__));
@@ -1344,14 +1329,9 @@ HIFDiagWriteMem(HIF_DEVICE *hif_device, A_UINT32 address, A_UINT8 *data, int nby
      *   2) Buffer in DMA-able space
      */
     orig_nbytes = nbytes;
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     data_buf = (A_UCHAR *)pci_alloc_consistent(scn->sc_osdev->bdev,
                                              orig_nbytes,
                                              &CE_data_base);
-#else
-    data_buf = (A_UCHAR *)dma_alloc_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                             orig_nbytes, &CE_data_base, GFP_ATOMIC);
-#endif
     if (!data_buf) {
         status = A_NO_MEMORY;
         goto done;
@@ -1359,12 +1339,7 @@ HIFDiagWriteMem(HIF_DEVICE *hif_device, A_UINT32 address, A_UINT8 *data, int nby
 
     /* Copy caller's data to allocated DMA buf */
     A_MEMCPY(data_buf, data, orig_nbytes);
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     pci_dma_sync_single_for_device(scn->sc_osdev->bdev, CE_data_base, orig_nbytes, PCI_DMA_TODEVICE);
-#else
-    dma_sync_single_for_device(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                               CE_data_base, orig_nbytes, DMA_TO_DEVICE);
-#endif
 
     /*
      * The address supplied by the caller is in the
@@ -1452,13 +1427,8 @@ done:
     A_TARGET_ACCESS_UNLIKELY(targid);
 
     if (data_buf) {
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
         pci_free_consistent(scn->sc_osdev->bdev, orig_nbytes,
                          data_buf, CE_data_base);
-#else
-        dma_free_coherent(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                          orig_nbytes, data_buf, CE_data_base);
-#endif
     }
 
     if (status != A_OK) {
@@ -1614,13 +1584,8 @@ hif_post_recv_buffers_for_pipe(struct HIF_CE_pipe_info *pipe_info)
 
         CE_data = adf_nbuf_get_frag_paddr_lo(nbuf, 0);
 
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
         pci_dma_sync_single_for_device(scn->sc_osdev->bdev, CE_data,
                                        buf_sz, PCI_DMA_FROMDEVICE);
-#else
-        dma_sync_single_for_device(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                   CE_data, buf_sz, DMA_FROM_DEVICE);
-#endif
         status = CE_recv_buf_enqueue(ce_hdl, (void *)nbuf, CE_data);
         A_ASSERT(status == EOK);
         if (status != EOK) {
@@ -2110,12 +2075,7 @@ HIFExchangeBMIMsg(HIF_DEVICE *hif_device,
         transaction->bmi_response_host = bmi_response;
         transaction->bmi_response_CE = CE_response;
         /* dma_cache_sync(dev, bmi_response, BMI_DATASZ_MAX, DMA_FROM_DEVICE); */
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
         pci_dma_sync_single_for_device(scn->sc_osdev->bdev, CE_response, BMI_DATASZ_MAX, PCI_DMA_FROMDEVICE);
-#else
-        dma_sync_single_for_device(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                   CE_response, BMI_DATASZ_MAX, DMA_FROM_DEVICE);
-#endif
         CE_recv_buf_enqueue(ce_recv, transaction, transaction->bmi_response_CE);
         /* NB: see HIF_BMI_recv_done */
     } else {
@@ -2124,12 +2084,7 @@ HIFExchangeBMIMsg(HIF_DEVICE *hif_device,
     }
 
     /* dma_cache_sync(dev, bmi_request, request_length, DMA_TO_DEVICE); */
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     pci_dma_sync_single_for_device(scn->sc_osdev->bdev, CE_request, request_length, PCI_DMA_TODEVICE);
-#else
-        dma_sync_single_for_device(&((struct pci_dev *)scn->sc_osdev->bdev)->dev,
-                                   CE_request, request_length, DMA_TO_DEVICE);
-#endif
 
     status = CE_send(ce_send, transaction, CE_request, request_length, -1, 0);
     ASSERT(status == EOK);
diff --git a/CORE/SERVICES/HIF/PCIe/if_pci.c b/CORE/SERVICES/HIF/PCIe/if_pci.c
index dd4c47c..4fd2713 100644
--- a/CORE/SERVICES/HIF/PCIe/if_pci.c
+++ b/CORE/SERVICES/HIF/PCIe/if_pci.c
@@ -1665,39 +1665,23 @@ again:
 #ifdef CONFIG_ARM_LPAE
     /* if CONFIG_ARM_LPAE is enabled, we have to set 64 bits mask
      * for 32 bits device also. */
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret =  pci_set_dma_mask(pdev, DMA_BIT_MASK(64));
-#else
-    ret = dma_set_mask(&pdev->dev, DMA_BIT_MASK(64));
-#endif
     if (ret) {
         printk(KERN_ERR "ath: Cannot enable 64-bit pci DMA\n");
         goto err_dma;
     }
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret = pci_set_consistent_dma_mask(pdev, DMA_BIT_MASK(64));
-#else
-    ret = dma_set_coherent_mask(&pdev->dev, DMA_BIT_MASK(64));
-#endif
     if (ret) {
         printk(KERN_ERR "ath: Cannot enable 64-bit consistent DMA\n");
         goto err_dma;
     }
 #else
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret = pci_set_dma_mask(pdev, DMA_BIT_MASK(32));
-#else
-    ret = dma_set_mask(&pdev->dev, DMA_BIT_MASK(32));
-#endif
     if (ret) {
         printk(KERN_ERR "ath: Cannot enable 32-bit pci DMA\n");
         goto err_dma;
     }
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret = pci_set_consistent_dma_mask(pdev, DMA_BIT_MASK(32));
-#else
-    ret = dma_set_coherent_mask(&pdev->dev, DMA_BIT_MASK(32));
-#endif
     if (ret) {
         printk(KERN_ERR "%s: Cannot enable 32-bit consistent DMA!\n",
                __func__);
@@ -2035,39 +2019,23 @@ again:
 #ifdef CONFIG_ARM_LPAE
     /* if CONFIG_ARM_LPAE is enabled, we have to set 64 bits mask
      * for 32 bits device also. */
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret =  pci_set_dma_mask(pdev, DMA_BIT_MASK(64));
-#else
-    ret = dma_set_mask(&pdev->dev, DMA_BIT_MASK(64));
-#endif
     if (ret) {
         printk(KERN_ERR "ath: Cannot enable 64-bit pci DMA\n");
         goto err_dma;
     }
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret = pci_set_consistent_dma_mask(pdev, DMA_BIT_MASK(64));
-#else
-    ret = dma_set_coherent_mask(&pdev->dev, DMA_BIT_MASK(64));
-#endif
     if (ret) {
         printk(KERN_ERR "ath: Cannot enable 64-bit consistent DMA\n");
         goto err_dma;
     }
 #else
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret = pci_set_dma_mask(pdev, DMA_BIT_MASK(32));
-#else
-    ret = dma_set_mask(&pdev->dev, DMA_BIT_MASK(32));
-#endif
     if (ret) {
         printk(KERN_ERR "ath: Cannot enable 32-bit pci DMA\n");
         goto err_dma;
     }
-#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 18, 0)
     ret = pci_set_consistent_dma_mask(pdev, DMA_BIT_MASK(32));
-#else
-    ret = dma_set_coherent_mask(&pdev->dev, DMA_BIT_MASK(32));
-#endif
     if (ret) {
         printk(KERN_ERR "%s: Cannot enable 32-bit consistent DMA!\n",
                __func__);
diff --git a/CORE/SERVICES/HIF/ath_procfs.c b/CORE/SERVICES/HIF/ath_procfs.c
index 259a17e..d5976f9 100644
--- a/CORE/SERVICES/HIF/ath_procfs.c
+++ b/CORE/SERVICES/HIF/ath_procfs.c
@@ -65,11 +65,11 @@ static void *get_hif_hdl_from_file(struct file *file)
 #endif
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(3,10,0))
 #if defined(HIF_PCI)
-	scn = (struct hif_pci_softc *)PDE_DATA(file_inode(file));
+	scn = (struct hif_pci_softc *)pde_data(file_inode(file));
 #elif defined(HIF_USB)
-	scn = (struct hif_usb_softc *)PDE_DATA(file_inode(file));
+	scn = (struct hif_usb_softc *)pde_data(file_inode(file));
 #elif defined(HIF_SDIO)
-	scn = (struct ath_hif_sdio_softc *)PDE_DATA(file_inode(file));
+	scn = (struct ath_hif_sdio_softc *)pde_data(file_inode(file));
 #endif
 #else
 #if defined(HIF_PCI)
diff --git a/CORE/SERVICES/HIF/sdio/hif_sdio_recv.c b/CORE/SERVICES/HIF/sdio/hif_sdio_recv.c
index 4c1610c..9fbebfa 100644
--- a/CORE/SERVICES/HIF/sdio/hif_sdio_recv.c
+++ b/CORE/SERVICES/HIF/sdio/hif_sdio_recv.c
@@ -1028,7 +1028,7 @@ int rx_completion_task(void *param)
         }
         adf_os_spin_unlock_irqrestore(&device->pRecvTask->rx_alloc_lock);
     }
-    complete_and_exit(&device->pRecvTask->rx_completion_exit, 0);
+    kthread_complete_and_exit(&device->pRecvTask->rx_completion_exit, 0);
     return 0;
 }
 #endif
@@ -1844,4 +1844,3 @@ int HIFDevDsrHandler(void *context)
     AR_DEBUG_PRINTF(ATH_DEBUG_IRQ, ("-DevDsrHandler \n"));
     return status;
 }
-
diff --git a/CORE/SERVICES/HIF/sdio/linux/native_sdio/src/hif.c b/CORE/SERVICES/HIF/sdio/linux/native_sdio/src/hif.c
index 6e2f5b8..61f7d3f 100644
--- a/CORE/SERVICES/HIF/sdio/linux/native_sdio/src/hif.c
+++ b/CORE/SERVICES/HIF/sdio/linux/native_sdio/src/hif.c
@@ -832,7 +832,7 @@ static int tx_completion_task(void *param)
 	while (device->tx_completion_req != NULL)
 		tx_clean_completion_list(device);
 
-	complete_and_exit(&device->tx_completion_exit, 0);
+	kthread_complete_and_exit(&device->tx_completion_exit, 0);
 	return 0;
 }
 
@@ -1068,7 +1068,7 @@ static int async_task(void *param)
         sdio_release_host(device->func);
     }
 
-    complete_and_exit(&device->async_completion, 0);
+    kthread_complete_and_exit(&device->async_completion, 0);
     return 0;
 }
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(2,6,32))
diff --git a/CORE/SVC/src/logging/wlan_logging_sock_svc.c b/CORE/SVC/src/logging/wlan_logging_sock_svc.c
index 97645aa..8c99776 100644
--- a/CORE/SVC/src/logging/wlan_logging_sock_svc.c
+++ b/CORE/SVC/src/logging/wlan_logging_sock_svc.c
@@ -782,7 +782,7 @@ static int wlan_logging_thread(void *Arg)
 	}
 
 
-	complete_and_exit(&gwlan_logging.shutdown_comp, 0);
+	kthread_complete_and_exit(&gwlan_logging.shutdown_comp, 0);
 
 	return 0;
 }
diff --git a/CORE/SYS/legacy/src/system/inc/sysDebug.h b/CORE/SYS/legacy/src/system/inc/sysDebug.h
index 8cf24f9..0a023fa 100644
--- a/CORE/SYS/legacy/src/system/inc/sysDebug.h
+++ b/CORE/SYS/legacy/src/system/inc/sysDebug.h
@@ -37,7 +37,7 @@
 #ifndef __SYS_DEBUG_H__
 #define __SYS_DEBUG_H__
 
-#include <stdarg.h>
+#include <linux/stdarg.h>
 # include "utilsApi.h"
 # include "sirDebug.h"
 # include "sirParams.h"
diff --git a/CORE/SYS/legacy/src/utils/inc/utilsParser.h b/CORE/SYS/legacy/src/utils/inc/utilsParser.h
index aa2da85..0293aac 100644
--- a/CORE/SYS/legacy/src/utils/inc/utilsParser.h
+++ b/CORE/SYS/legacy/src/utils/inc/utilsParser.h
@@ -39,7 +39,7 @@
 #ifndef __UTILS_PARSE_H__
 #define __UTILS_PARSE_H__
 
-#include <stdarg.h>
+#include <linux/stdarg.h>
 #include "sirApi.h"
 #include "dot11f.h"
 #include "utilsApi.h"
diff --git a/CORE/SYS/legacy/src/utils/src/dot11f.c b/CORE/SYS/legacy/src/utils/src/dot11f.c
index 7b82bd5..9b74749 100644
--- a/CORE/SYS/legacy/src/utils/src/dot11f.c
+++ b/CORE/SYS/legacy/src/utils/src/dot11f.c
@@ -118,7 +118,7 @@ typedef struct sIEDefn {
 #ifndef DOT11F_HAVE_LOG_MACROS
 
 #include <stdio.h>
-#include <stdarg.h>
+#include <linux/stdarg.h>
 
 #ifndef DOT11F_LOG_GATE
 #   define DOT11F_LOG_GATE FRLOGW
diff --git a/CORE/SYS/legacy/src/utils/src/logApi.c b/CORE/SYS/legacy/src/utils/src/logApi.c
index 5763688..ba12329 100644
--- a/CORE/SYS/legacy/src/utils/src/logApi.c
+++ b/CORE/SYS/legacy/src/utils/src/logApi.c
@@ -41,7 +41,7 @@
 #include <utilsApi.h>
 #include <wlan_qct_wda.h>
 
-#include <stdarg.h>
+#include <linux/stdarg.h>
 #include "utilsGlobal.h"
 #include "macInitApi.h"
 #include "palApi.h"
diff --git a/CORE/UTILS/PKTLOG/linux_ac.c b/CORE/UTILS/PKTLOG/linux_ac.c
index 05922b2..e8f55db 100644
--- a/CORE/UTILS/PKTLOG/linux_ac.c
+++ b/CORE/UTILS/PKTLOG/linux_ac.c
@@ -370,12 +370,16 @@ static int pktlog_sysctl_register(struct ol_softc *scn)
 	set_ctl_name(0, CTL_AUTO);
 	pl_info_lnx->sysctls[0].procname = PKTLOG_PROC_DIR;
 	pl_info_lnx->sysctls[0].mode = PKTLOG_PROCSYS_DIR_PERM;
+#if (LINUX_VERSION_CODE < KERNEL_VERSION(6,5,0))
 	pl_info_lnx->sysctls[0].child = &pl_info_lnx->sysctls[2];
+#endif
 	/* [1] is NULL terminator */
 	set_ctl_name(2, CTL_AUTO);
 	pl_info_lnx->sysctls[2].procname = proc_name;
 	pl_info_lnx->sysctls[2].mode = PKTLOG_PROCSYS_DIR_PERM;
+#if (LINUX_VERSION_CODE < KERNEL_VERSION(6,5,0))
 	pl_info_lnx->sysctls[2].child = &pl_info_lnx->sysctls[4];
+#endif
 	/* [3] is NULL terminator */
 	set_ctl_name(4, CTL_AUTO);
 	pl_info_lnx->sysctls[4].procname = "enable";
@@ -444,7 +448,11 @@ static int pktlog_sysctl_register(struct ol_softc *scn)
 
 	/* and register everything */
 	/* register_sysctl_table changed from 2.6.21 onwards */
-#if (LINUX_VERSION_CODE > KERNEL_VERSION(2,6,20))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6,5,0))
+  // TODO
+	pl_info_lnx->sysctl_header = NULL;
+	return 0;
+#elif (LINUX_VERSION_CODE > KERNEL_VERSION(2,6,20))
 	pl_info_lnx->sysctl_header =
 			register_sysctl_table(pl_info_lnx->sysctls);
 #else
@@ -776,7 +784,7 @@ __pktlog_read(struct file *file, char *buf, size_t nbytes, loff_t *ppos)
 	int fold_offset, ppos_data, cur_rd_offset;
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(3,10,0)
 	struct ath_pktlog_info *pl_info = (struct ath_pktlog_info *)
-					  PDE_DATA(file->f_path.dentry->d_inode);
+					  pde_data(file->f_path.dentry->d_inode);
 #else
 	struct proc_dir_entry *proc_entry = PDE(file->f_dentry->d_inode);
 	struct ath_pktlog_info *pl_info = (struct ath_pktlog_info *)
@@ -911,7 +919,7 @@ pktlog_read(struct file *file, char *buf, size_t nbytes, loff_t *ppos)
 	size_t ret_val = 0;
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(3,10,0)
 	struct ath_pktlog_info *pl_info = (struct ath_pktlog_info *)
-					  PDE_DATA(file->f_path.dentry->d_inode);
+					  pde_data(file->f_path.dentry->d_inode);
 #else
 	struct proc_dir_entry *proc_entry = PDE(file->f_dentry->d_inode);
 	struct ath_pktlog_info *pl_info = (struct ath_pktlog_info *)
diff --git a/CORE/VOSS/inc/vos_trace.h b/CORE/VOSS/inc/vos_trace.h
index f5bc981..ad53386 100644
--- a/CORE/VOSS/inc/vos_trace.h
+++ b/CORE/VOSS/inc/vos_trace.h
@@ -44,7 +44,7 @@
   Include Files
   ------------------------------------------------------------------------*/
 #include  <vos_types.h>  // For VOS_MODULE_ID...
-#include  <stdarg.h>       // For va_list...
+#include  <linux/stdarg.h>       // For va_list...
 #include  <vos_status.h>
 #include  <i_vos_types.h>
 
diff --git a/CORE/VOSS/src/vos_nvitem.c b/CORE/VOSS/src/vos_nvitem.c
index b8119e9..9ab2732 100644
--- a/CORE/VOSS/src/vos_nvitem.c
+++ b/CORE/VOSS/src/vos_nvitem.c
@@ -60,6 +60,7 @@
 #include <net/regulatory.h>
 #endif
 
+
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(3,9,0)) && !defined(WITH_BACKPORTS)
 #define IEEE80211_CHAN_NO_80MHZ		1<<7
 #endif
@@ -1539,7 +1540,7 @@ VOS_STATUS vos_nv_getRegDomainFromCountryCode( v_REGDOMAIN_t *pRegDomain,
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(5, 4, 0)) || !defined(CLD_REGDB)
         INIT_COMPLETION(pHddCtx->reg_init);
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(3,9,0)) || defined(WITH_BACKPORTS)
-        regulatory_hint_user(country_code, NL80211_USER_REG_HINT_USER);
+        regulatory_hint(wiphy, country_code); //, NL80211_USER_REG_HINT_USER);
 #else
         regulatory_hint_user(country_code);
 #endif
@@ -2709,4 +2710,3 @@ VOS_STATUS vos_init_wiphy_from_nv_bin(void)
 
     return VOS_STATUS_SUCCESS;
 }
-
diff --git a/CORE/VOSS/src/vos_sched.c b/CORE/VOSS/src/vos_sched.c
index a0a269d..1f4928d 100644
--- a/CORE/VOSS/src/vos_sched.c
+++ b/CORE/VOSS/src/vos_sched.c
@@ -976,7 +976,7 @@ VosMCThread
   // If we get here the MC thread must exit
   VOS_TRACE( VOS_MODULE_ID_VOSS, VOS_TRACE_LEVEL_ERROR,
       "%s: MC Thread exiting!!!!", __func__);
-  complete_and_exit(&pSchedContext->McShutdown, 0);
+  kthread_complete_and_exit(&pSchedContext->McShutdown, 0);
 } /* VosMCThread() */
 
 v_BOOL_t isWDresetInProgress(void)
@@ -1268,7 +1268,7 @@ VosWDThread
   // If we get here the Watchdog thread must exit
   VOS_TRACE( VOS_MODULE_ID_VOSS, VOS_TRACE_LEVEL_INFO,
       "%s: Watchdog Thread exiting !!!!", __func__);
-  complete_and_exit(&pWdContext->WdShutdown, 0);
+  kthread_complete_and_exit(&pWdContext->WdShutdown, 0);
 
 err_reset:
     VOS_TRACE( VOS_MODULE_ID_VOSS, VOS_TRACE_LEVEL_FATAL,
@@ -1558,7 +1558,7 @@ static int VosTlshimRxThread(void *arg)
 
    VOS_TRACE(VOS_MODULE_ID_VOSS, VOS_TRACE_LEVEL_ERROR,
              "%s: Exiting VOSS Tlshim rx thread", __func__);
-   complete_and_exit(&pSchedContext->TlshimRxShutdown, 0);
+   kthread_complete_and_exit(&pSchedContext->TlshimRxShutdown, 0);
 }
 #endif
 
diff --git a/CORE/VOSS/src/vos_utils.c b/CORE/VOSS/src/vos_utils.c
index f9a2008..255f433 100644
--- a/CORE/VOSS/src/vos_utils.c
+++ b/CORE/VOSS/src/vos_utils.c
@@ -88,7 +88,7 @@
 #define AAD_LEN 20
 #define IV_SIZE_AES_128 16
 #define CMAC_IPN_LEN 6
-
+#define CRYPTO_ALG_TYPE_AHASH_MASK	0x0000000e
 
 /*----------------------------------------------------------------------------
  * Type Declarations
@@ -696,7 +696,7 @@ int hmac_sha1(v_U8_t *key, v_U8_t ksize, char *plaintext, v_U8_t psize,
     }
 
     ahash_request_set_callback(req, CRYPTO_TFM_REQ_MAY_BACKLOG,
-                               hmac_sha1_complete, &tresult);
+                               (crypto_completion_t)hmac_sha1_complete, &tresult);
 
     hash_buff = kzalloc(psize, GFP_KERNEL);
     if (!hash_buff) {
@@ -869,7 +869,7 @@ int hmac_md5(v_U8_t *key, v_U8_t ksize, char *plaintext, v_U8_t psize,
     }
 
     ahash_request_set_callback(req, CRYPTO_TFM_REQ_MAY_BACKLOG,
-                                        hmac_md5_complete, &tresult);
+                                        (crypto_completion_t)hmac_md5_complete, &tresult);
 
     hash_buff = kzalloc(psize, GFP_KERNEL);
     if (!hash_buff) {
diff --git a/CORE/WDA/src/wlan_qct_wda_debug.h b/CORE/WDA/src/wlan_qct_wda_debug.h
index d741d7b..d42b361 100644
--- a/CORE/WDA/src/wlan_qct_wda_debug.h
+++ b/CORE/WDA/src/wlan_qct_wda_debug.h
@@ -30,7 +30,7 @@
 #if  !defined (ANI_OS_TYPE_ANDROID)
 #include <stdio.h>
 #endif
-#include <stdarg.h>
+#include <linux/stdarg.h>
 
 #include "utilsApi.h"
 #include "sirDebug.h"
diff --git a/Kbuild b/Kbuild
index 26464cd..dcf8bc2 100644
--- a/Kbuild
+++ b/Kbuild
@@ -104,7 +104,7 @@ ifeq ($(KERNEL_BUILD), 0)
 
 	ifneq ($(CONFIG_MOBILE_ROUTER), y)
 	#Flag to enable TDLS feature
-	CONFIG_QCOM_TDLS := y
+	#CONFIG_QCOM_TDLS := y
 	endif
 
 	#Flag to enable Fast Transition (11r) feature
@@ -1107,10 +1107,6 @@ ifeq ($(CONFIG_WLAN_POWER_DEBUGFS), y)
 CDEFINES += -DWLAN_POWER_DEBUGFS
 endif
 
-ifneq ($(CONFIG_HDD_WLAN_START_WAIT_TIME),)
-CDEFINES += -DHDD_WLAN_START_WAIT_TIME=$(CONFIG_HDD_WLAN_START_WAIT_TIME)
-endif
-
 ifeq ($(CONFIG_FEATURE_COEX_PTA_CONFIG_ENABLE), y)
 CDEFINES += -DFEATURE_COEX_PTA_CONFIG_ENABLE
 endif
@@ -1167,7 +1163,7 @@ endif
 
 ifeq ($(CONFIG_TXRX_PERF_EXT), y)
 CDEFINES += -DCONFIG_IXC_TIMER \
-		-DCONFIG_PERF_MODE 
+		-DCONFIG_PERF_MODE
 endif
 
 ifeq ($(CONFIG_CUSTOMIZED_FIRMWARE_PATH), y)
diff --git a/Makefile b/Makefile
index 401b660..777006e 100644
--- a/Makefile
+++ b/Makefile
@@ -17,9 +17,11 @@ KBUILD_OPTIONS += CONFIG_QCA_WIFI_2_0=1
 KBUILD_OPTIONS += $(WLAN_SELECT)
 KBUILD_OPTIONS += WLAN_OPEN_SOURCE=$(WLAN_OPEN_SOURCE)
 KBUILD_OPTIONS += $(KBUILD_EXTRA) # Extra config if any
+KBUILD_OPTIONS += CONFIG_CLD_HL_SDIO_CORE=y
+KBUILD_OPTIONS += CONFIG_FORCE_MLO_SUPPORT=y
 
 all:
-	$(MAKE) -C $(KERNEL_SRC) M=$(CURDIR) modules $(KBUILD_OPTIONS)
+	$(MAKE) -j $(shell nproc) -C $(KERNEL_SRC) M=$(CURDIR) modules $(KBUILD_OPTIONS)
 
 modules_install:
 	$(MAKE) INSTALL_MOD_STRIP=1 -C $(KERNEL_SRC) M=$(CURDIR) modules_install
